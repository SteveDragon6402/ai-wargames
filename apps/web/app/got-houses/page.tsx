"use client";

import GameCore from "./components/GameCore";

/** Standalone / admin play — no room, no persistence, admin mode on. */
export default function GotHousesPage() {
  return <GameCore />;
}
