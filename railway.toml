[build]
builder = "nixpacks"

[[services]]
name = "web"
buildCommand = "NODE_ENV=production pnpm install --frozen-lockfile && NODE_ENV=production pnpm build"
startCommand = "pnpm --filter @wargame/web start"
